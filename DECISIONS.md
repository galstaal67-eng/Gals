# יומן החלטות אפיון — מערכת SOX

> מסמך זה מרכז את ההכרעות התכנוניות שהתקבלו על בסיס מסמך האפיון
> ("אפיון מערכת SOX", 01/2026). הוא מהווה בסיס לחתימת אדריכל המערכת
> **לפני** תחילת הפיתוח. כל שינוי בהחלטה כאן מחייב עדכון גם ב-`SPEC.md`.

**גרסה:** 1.0
**תאריך:** 2026-06-02
**סטטוס:** ממתין לחתימת אדריכל מערכת

---

## 1. סיכום ההחלטות

| מזהה | נושא | החלטה | סוג |
|------|------|--------|-----|
| C1 | מודל מהותיות | טבלת `materiality_parameters` (1..N לכל שנת ביקורת/חברה) | תיקון סתירה |
| C2 | סטטוסי טסט | enum אחיד של 12 סטטוסים (ראה §3) | תיקון סתירה |
| C3 | אסטרטגיית זהות | פיצול: Entra ID ליועצי אנטרופי · Local + 2FA ללקוחות ורו"ח מבקר | אסטרטגי |
| C4 | פלטפורמת ענן | Azure | אסטרטגי |
| C5 | Frontend | React + TypeScript (תמיכת RTL + i18n) | אסטרטגי |
| C6 | הפניות צולבות שבורות | ניקוי ותיקון במסגרת `SPEC.md` | תחזוקה |
| Q1 | Multi-tenancy | PostgreSQL Row-Level Security (RLS) לפי `tenant_id` מ-JWT | טכני |
| Q2 | קליטת מייל | Microsoft Graph + Subscriptions (webhook) | טכני |
| Q3 | השוואת ראיות | hash + metadata ב-MVP · דמיון תוכני (LLM) בשלב ב' | טכני |
| Q4 | ספק AI (שלב ב') | Anthropic Claude, מאחורי שכבת הפשטה (ללא lock-in) | אסטרטגי |
| Q5 | Retention קבצים | 7 שנים (דרישת SOX) — טעון אישור משפטי | טכני/משפטי |
| Q6 | בעלות בנקים | גלובלי + override לכל לקוח | טכני |
| Q7 | מובייל | Responsive בלבד ב-MVP (לא PWA) | טכני |
| Q8 | שפה | i18n + RTL מהיום הראשון (עברית + אנגלית) | טכני |
| C7 | מודל תהליכים/סיכונים | בנק (קטלוג גלובלי/לקוח) + טבלאות מופעים לכל חברת-בת/שנה | אסטרטגי (Phase 2) |
| C8 | סף איכותי חברת בת | "יותר מ-3 כן" = **≥4** תשובות "כן" (מתוך 5) → מהותי איכותית | דומיין (Phase 2) |

---

## 2. הנמקות מפורטות

### C1 — מודל מהותיות
**הסתירה:** גוף האפיון מתאר שלושה פרמטרים לסף מהותיות (סף 1/2/3, כל אחד עם
פרמטר נבחר, ערך מהדוחות הכספיים, אחוז מהותיות וחישוב אוטומטי = ערך × אחוז),
בעוד הנספח הטכני הגדיר שדה בודד `materiality_threshold` ב-`clients` וב-`subsidiaries`.

**ההחלטה:** יצירת טבלה נפרדת `materiality_parameters` המחזיקה 1..N רשומות לכל
שנת ביקורת ולכל חברה. כל רשומה: סוג פרמטר (מתוך רשימה סגורה), ערך, אחוז,
וסף מחושב (read-only). הדבר מאפשר את ה-3 שבאפיון וגמישות עתידית.

### C2 — סטטוסי טסט
**הסתירה:** ה-UI מגדיר כ-12 סטטוסים שמניעים את ה-workflow, מכונת המצב בנספח
הגדירה ~6 בלבד.
**ההחלטה:** רשימת ה-UI היא המוסמכת. enum אחיד של 12 סטטוסים (§3), ומכונת
המצב הורחבה בהתאם (`SPEC.md`). `דורש טיפול` ו`ליקוי` הם סטטוסים **נפרדים**.
`חומרת הליקוי` (ליקוי / ליקוי מהותי / חולשה מהותית) הוא שדה תיאור חומרה —
**אינו** סטטוס.

### C3 — אסטרטגיית זהות
יועצי אנטרופי ומשתמשים פנימיים מזדהים מול **Entra ID (Azure AD) + SSO**.
לקוחות חיצוניים ורו"ח מבקר מזדהים עם **שם משתמש + סיסמה + 2FA** (local).
תואם את שני האזכורים במסמך (גישה ישירה עם סיסמה אישית מצד אחד, ו-AD/SSO
ארגוני מצד שני). מודל הנתונים תומך בכך דרך `tenants.ad_tenant_id` ו-`users.mfa_enabled`.

### C4 — Azure
כל הרמזים במסמך מצביעים על Azure: Microsoft Graph לקליטת מייל, Azure AD,
WAF של Microsoft, וקישורי SharePoint/Planner. נעילה ל-Azure מפשטת זהות, מייל
ואבטחה לסטאק אחד. אחסון קבצים: **Azure Blob Storage**.

### C5 — React + TypeScript
אקוסיסטם רחב, תמיכת RTL/i18n בוגרת, התאמה לתבנית העיצוב NobleUI (Bootstrap 5)
המוזכרת במסמך. TypeScript לאיכות ותחזוקתיות.

### Q4 — Anthropic Claude + סיכון data-residency
**הערה חשובה:** Claude אינו שירות נייטיב של Azure. הגישה אליו בשלב ב' תהיה
דרך Anthropic API ישירות או דרך AWS Bedrock / Google Vertex — כלומר מידע
שיישלח ל-AI ייצא מסביבת ה-Azure אל ספק חיצוני.
**מענה נדרש בשלב ב':** DPA עם Anthropic; ודאות שאין אימון על נתוני ה-API
(ברירת המחדל ב-Anthropic API — אין אימון); שקילת anonymization לפני שליחה.
שכבת ההפשטה מאפשרת מעבר ל-Azure OpenAI אם data-residency יתברר כחוסם.
**אינו חוסם MVP** — ה-MVP אינו כולל AI כלל.

---

## 3. enum סטטוסי טסט (C2) — מוסמך

| # | סטטוס | הערה |
|---|--------|------|
| 1 | טרם התקבל | ברירת מחדל |
| 2 | לטיפול היועץ | |
| 3 | בהשלמת החברה | |
| 4 | ראיות נוספות לבחינה | |
| 5 | בקרה נבדקה ואושרה | |
| 6 | בקרה פנימית סגורה | |
| 7 | דורש טיפול | סטטוס עצמאי |
| 8 | ליקוי פתוח | |
| 9 | ליקוי עם בקרה מפצה | |
| 10 | ליקוי סגור | |
| 11 | ייבדק בסבב ב' | |
| 12 | לא רלוונטי | |

---

## 4. סיכונים ופריטים פתוחים לאישור

| # | פריט | אחראי | סטטוס |
|---|------|--------|--------|
| R1 | אישור משפטי ל-retention של 7 שנים (Q5) | לקוח / יועמ"ש | פתוח |
| R2 | data-residency ל-Claude בשלב ב' — DPA + מדיניות יציאת מידע (Q4) | אדריכל + לקוח | פתוח (שלב ב') |
| R3 | חתימת אדריכל מערכת על מסמך זה ועל `SPEC.md` | אדריכל | פתוח |

---

## 5. החלטות שנגזרות אך לא נשאלו במפורש

- **אחסון קבצים:** Azure Blob Storage, עם `file_hash` (SHA-256), ללא מחיקה
  פיזית (החלפה בלבד) — דרישת SOX.
- **Audit log:** append-only, ללא UPDATE/DELETE.
- **Soft-delete:** שדה `deleted_at` בכל הטבלאות (דרישת SOX).
- **מזהים בקוד באנגלית**, שמות ישויות בעברית רק ב-UI/i18n.

---

## 6. נימוקי Phase 2

### C7 — בנק + מופעים (תהליכים/סיכונים)
הנספח הטכני הגדיר `processes`/`risks` כטבלאות בנק יחידות, אך הטאבים בפועל
בוחרים תהליך/סיכון מהבנק ויוצרים **מופע** לכל חברת-בת בשנת ביקורת.
לכן נבנו שתי שכבות: טבלאות בנק (`processes`, `sub_activities`, `risks` —
גלובלי `is_global=true` או override לכל לקוח, Q6) וטבלאות מופעים
(`process_selections`, `risk_selections`). מאפשר שכפול משנה קודמת, שפה אחידה,
ושמירת היסטוריה לכל שנה. שדות הדירוג בסיכון רלוונטיים רק לתהליך שאינו ITGC.

### C8 — סף איכותי לחברת בת
האפיון: "במידה ויותר מ-3 שאלות מקבלות מענה 'כן' — ההמלצה האיכותית 'כן'".
פירוש מילולי: **≥4 מתוך 5** "כן" → מהותי איכותית. מיושם כקבוע יחיד
(`QUALITATIVE_YES_THRESHOLD=4`) הניתן לשינוי במקום אחד.

---

## 7. סטטוס פאזות
- ✅ **Phase 1** — לקוחות, משתמשים, Auth (Entra/Local+2FA), אנשי קשר, tenants,
  audit log, RLS, RBAC, i18n + Frontend React. bootstrap CLI + SSO JWKS.
- ✅ **Phase 2** — שנות ביקורת + ספי מהותיות (C1), חברות בנות (איכותי/סקופ),
  תהליכים (בנק+מופעים+CSV), סיכונים (בנק+מופעים).
- ✅ **Phase 3** — בקרות ותיקופים + מכונת מצב (draft→needs_validation→validated).
- ✅ **Phase 4** — טסטים (12 סטטוסים + FSM) + ראיות (החלפה ללא מחיקה).
- ✅ **Phase 5** — דשבורדים + התראות + דוחות (CSV/XLSX) + זרימות מייל.
- ✅ **Phase 6** — AI (המלצות בקרות, שכבת הפשטה Q4), השוואת ראיות (hash, Q3),
  קליטת מייל נכנס (webhook, Q2).

### פערי תשתית פתוחים (לא חוסמים)
- dispatcher מייל בפועל (worker שקורא `email_messages` queued ושולח דרך Graph).
- Azure Blob בפועל (כרגע אחסון לוקאלי מאחורי אותו ממשק `.save()`).
- ספק AI Anthropic בפועל (R2: DPA + data-residency) — כרגע ברירת מחדל heuristic.
